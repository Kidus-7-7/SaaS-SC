declare module '@supabase/supabase-js' {
  export const supabase: any; // Replace 'any' with the actual type if known
  export const checkSession: () => Promise<any>; // Replace 'any' with the actual return type if known
}
